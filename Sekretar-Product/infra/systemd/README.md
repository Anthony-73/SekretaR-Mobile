# Systemd

Systemd service definitions for a physical server can live here later.

Expected services:

- product API;
- AI processing;
- worker;
- queue;
- web app serving layer;
- scheduled maintenance.

This folder currently contains architecture scaffolding only.
