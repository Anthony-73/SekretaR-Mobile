# Docker

Docker configuration will live here when implementation begins.

The target is a portable local-to-server runtime:

- local development;
- physical server deployment;
- repeatable service startup;
- separate volumes for database, queue, and storage.

No generated runtime data should be committed here.
