# sekretar_meaning

Framework-agnostic Meaning domain package.

Phase 1 scope:

```text
People -> Roles -> Responsibilities
```

Core flow:

```text
Reference -> Evidence -> Hypothesis -> Decision -> Entity -> Validation
```

This module defines the architectural domain surface only. Policy, service, and
persistence implementations are intentionally deferred.
