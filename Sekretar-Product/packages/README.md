# Packages

Shared packages live here.

Packages should contain reusable contracts and technical helpers, not product business logic.

Expected packages:

- `contracts` - shared API and event contracts.
- `config` - configuration loading conventions.
- `db` - migration and database access conventions.
- `storage` - storage abstraction.
- `observability` - logging, metrics, tracing conventions.
