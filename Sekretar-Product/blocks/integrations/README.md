# Integrations Block

Integrations contains external product connections.

Examples:

- Google Calendar;
- document export;
- future email integrations;
- future CRM or project tools;
- future external data providers.

Integration work should run through Worker when it can be slow, retryable, or dependent on external availability.
